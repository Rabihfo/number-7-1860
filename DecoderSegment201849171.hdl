CHIP DecoderSegment201849171 {
    IN A, B, C, D;
    OUT d;

    PARTS:
    Not(in=A, out=l);
    Not(in=B, out=k);
    Not(in=C, out=j);
    Not(in=D, out=h);

    And(a=k, b=h, out=xd);     // segment d
    And(a=l, b=k, out=yd);
    And(a=yd, b=D, out=zd);
    And(a=B, b=j, out=md);
    And(a=md, b=h, out=nd);
    And(a=A, b=k, out=pd);
    And(a=A, b=C, out=qd);

    Or(a=xd, b=zd, out=rd);
    Or(a=rd, b=nd, out=sd);
    Or(a=sd, b=pd, out=td);
    Or(a=td, b=qd, out=d);
}
